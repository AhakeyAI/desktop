# AhaKey Studio

Native Windows WPF utility, version 2.0.0-alpha.6.1 (file version 2.0.0.61); Phase 6.1 corrects the normal BLE product flow while preserving the accepted Phase 6 backend and established visual identity. Studio 2 is the internal generation name; the visible product is AhaKey Studio. Product authority is the approved user brief and docs/ Phase 0 audit. Assembly-derived `AppVersionService` supplies footer, About, Diagnostics, copied export and startup/support logs; commit/build is labeled separately from product version.

Acceptance correction: the user accepted Phase 6 backend capabilities but rejected product acceptance after normal BLE use hid key Apply, made accepted lighting unusable and exposed conflicting About/footer versions. The earlier Phase 6 completion assertion below and in historical phase documents is superseded as a product-acceptance claim; individually recorded ACKs and observations keep their original scope. Phase 6.1's software checks, USB handoff ACKs and actual Codex event traffic are recorded, but final physical K2 and normal Codex visual confirmations remain pending. No full Phase 6.1 product acceptance is claimed. See [direction](../../docs/studio2-phase61/ui-direction.md), [capability diagnosis](../../docs/studio2-phase61/capability-gating.md) and [normal-app evidence](../../docs/studio2-phase61/physical-acceptance.md).

Normal startup opens Profiles in Real mode. On first run it follows System theme/language (EN/RU/ZH, otherwise English) and invites the user to connect AhaKey. The picker shows device names and Bluetooth without Windows identifiers. A saved selection starts one bounded reconnect burst of at most two attempts; loss of a connected session also permits one bounded burst. Explicit Disconnect cancels recovery and retains selection; Forget clears selection. Errors never silently select Mock. Mock lives behind Advanced / Developer mode and retains simulated write/readback and safe Test behavior.

Users maintain four stable editing profiles and a local keymap draft persisted under %LOCALAPPDATA%\\AhaKey\\Studio2. Real telemetry is separate from configuration evidence: connection queries only status (00) and capabilities (9F), never reads a configuration snapshot, writes a physical configuration, enables Test, or reports Synced. Connecting never replaces the draft or selected editing profile. Missing fields remain unreported, and retained observations become stale after disconnect or 60 seconds. There is no continuous telemetry polling. Runtime EN/RU/ZH and System/Light/Dark remain supported.

The accepted Phase 3.1 AE1E evidence reports firmware 1.0 and a status-only 9F acknowledgement without capability fields. The alpha presents LegacyTelemetryOnly, Legacy / incomplete, Capabilities not reported, and configuration readback unavailable. Historical battery values are never defaults. Full-profile writes/readback remain unavailable. Phase4B permits explicit selected K2–K4 shortcut/label writes on the attested AD1E USB unit after exact command review; 04 is disclosed as a global save. K1 is never writable through this path. No verified compatible firmware package is offered. Phase 3.3 physical acceptance is tracked separately in `../../docs/studio2-phase33/`; older acceptance is not a substitute for running the published EXE.

One normal instance per user owns a local lock and current-user-only named pipe for activation. Logs rotate under the per-user Logs folder, and copied diagnostics redact device identifiers. The published AhaKey Studio.exe includes .NET 10, needs no developer launcher, bridge, or port 9000, and opens without a console. USB is an explicit second transport: unique vendor-defined HID collection, descriptor-confirmed report sizes and zero report ID, normal connection queries only00/9F; separately approved typed A1 controls cannot enter the read-only query API. A separate capability-gated A2 route now serves only the accepted static Display scope below; connection and read-only query APIs cannot invoke it. Switching transports stops reconnect intent and releases the previous session first; The Device-page USB route never auto-connects or falls back; explicit Phase 6.1 Apply may temporarily open one exact privately accepted USB candidate and restore eligible prior BLE. Firmware operations, 9D probes and unapproved setters remain excluded; accepted selected-key, RGB and exact-scope static Display controls use separate reviewed paths.

Windows desktop, C#, MVVM, x64. Retain IDs 0–3, the K1 F18 constraint, 160×80 client canvas and 8/12/12/12 current-Java software allocation proposal (not a proved physical layout). The fourth profile remains locally renamable. No extra design interview is required to resolve these explicit constraints.

Physical AD1E board photographs identify CH582M, 32.000 MHz, LR8203 A33 and PY25Q64HA (64 Mbit / 8 MiB), sufficient raw capacity for 176 x 28,672-byte display slots. The drawing is the same design/family with a differing/revised BOM. Capacity does not prove layout/migration/firmware compatibility; update remains NO-GO. Use Display / Animations and 0.96-inch color IPS in user-facing copy; Studio 2 abstractions use Display; the serialized Oled key remains for existing local drafts. AD1E physical acceptance is not generalized to AE1E. See ../../docs/studio2-phase34/physical-board-evidence.md.

Phase 3.5 adds an isolated one-shot USB characterization entry point, not a normal UI capability. The physical AD1E returned empty success ACKs for layout 9C and AI binding 94; at that checkpoint only default binding 83(profile 0) returned values (start 0, count 8, interval 100 ms, total frame slots 292). Firmware byte capacity and dimensions remain NOT_REPORTED; the 160×80 client format and photographed 8 MiB are separate evidence. At that Phase 3.5 checkpoint, physical Upload was unavailable and future display writes were NO-GO. Phase 5 later accepted only the static scope below; normal transport reads still stay 00/9F only. See [display contract](../../docs/studio2-phase35/display-contract.md).

Phase 4A adds host-only Claude Code, Cursor and Codex integration adapters; Kimi is unavailable pending a verified Windows contract. Config changes require explicit review and Apply, preserve unrelated entries and use local backups. Installed, Configured, Enabled, Trusted, Compatible and Service running remain independent evidence. The loopback service starts on request or, in Phase 6, after explicit persisted opt-in on app launch. Metadata event history does not retain prompt/tool payloads. Hardware AUTO starts off every session and requires a fresh connected physical status; physical feedback is a separate default-OFF opt-in, available only after effect acceptance. No Display feedback is sent.

Phase 4B completion retains the accepted AD1E USB K2 Ctrl+Enter ACK and user behavior verification, without readback. On the same accepted USB identity and firmware 1.0, a separately approved trial sent only RGB `91 01`, then `91 00` after eight seconds. Both were ACK-accepted; the user separately confirmed that the effect appeared and then the lights went off. Only numeric Effect 01 and Off 00 are accepted. Off does not restore an unknown earlier animation, and no persistent event mapping was proved. Acceptance binds to device/path, transport and firmware; USB proof is not copied to BLE or AE1E. Offline accepted-device screenshots are UI fixtures, never hardware proof. See [completion physical evidence](../../docs/studio2-phase4b-completion/physical-acceptance.md).

Lighting now offers a standalone numeric runtime-effect selector and explicit Preview, separately from the nine local event mappings. Physical feedback defaults OFF for each profile/integration but its explicit preference now persists across restart/reconnect, requires the matching live physical mode and one accepted route, deduplicates events for 30 seconds, and never replays or falls back. Failure pauses feedback for the failed connection without clearing the saved preference; reconnect or off/on restores eligibility subject to accepted device/firmware/profile gates. Hardware AUTO remains independent.

Keymap supports local focused capture and a manual HID shortcut builder with primary keys and left/right modifier variants. Escape is captured as a shortcut; explicit Cancel ends recording. Missing or whitespace-only local names display Record / Accept / Reject / Backspace for K1–K4; custom names remain unchanged. K1 stays Voice/F18 and excluded from the native writer. On 2026-09-20, the separately approved profile-2 K1 F19 → save → F18 → save experiment executed once. Baseline F18 and restored F18 were physically observed; F19 was not observed despite accepted write/save ACKs. K1 remains locked, and no second experiment is scheduled. This does not prove that remapping is impossible. See [Phase 5 K1 result](../../docs/studio2-phase5/k1-result.md). Brightness remains read-only: retained command 85 changes live brightness and marks configuration dirty, which a later save path can persist even without an immediate 04. No guaranteed transient trial exists for the actual firmware. See [capability decisions](../../docs/studio2-phase4b-completion/capability-decisions.md).

Integration rows expose a contextual Install / Repair / Start service / Stop service action when eligible, beside Details and More actions. Configuration changes still require concrete preview and Apply; native trust decisions remain external. Integrations handles PreviewMouseWheel through its parent scroll area so child controls do not trap page scrolling.

Display imports PNG/JPEG/BMP/GIF, offers Fit with black bars or Crop, previews the RGB565-quantized 160×80 output, identifies target profile/state and frame/byte totals, and exports a local software plan. The generic current-Java plan remains explicitly a proposal. A separately approved one-shot 00 → 83(0/1/2/3) → 00 read sequence measured Default bindings as (start,count,interval ms): profile 0=(0,8,100), 1=(8,1,100), 2=(9,1,100), 3=(0,0,0), each reporting 292 total slots. Status matched before/after; these are binding metadata, not pixel readback or a complete allocation map.

At the Phase 4B completion checkpoint, physical Display upload remained unavailable: unknown aliases/ownership and no specifically selected sacrificial asset prevented a first write. The user had waived preservation only for a specifically selected asset; slot 9 was then a candidate, not authorization. No K1, brightness, Display, firmware or persistent write occurred in that completion. This historical [Display decision](../../docs/studio2-phase4b-completion/display-decision.md) is superseded only by the narrow Phase 5 acceptance below.

Phase 5 live Codex evidence used existing legacy hooks through Studio normalization and policy, with no hook-file or native-trust changes. UserPromptSubmit produced RGB 01 and Stop produced 00, each once and ACK-accepted; the user observed the effect then lights off. Five events produced two physical operations and zero observed duplicates. Offline alias tests cover legacy/Studio suppression; simultaneous duplicate hooks were not physically tested. The retained contract has no event UUID, so the same normalized event/profile/integration/device session is conservatively coalesced for 30 seconds and may suppress legitimate repetitions. Runtime event state remains available; diagnostics contain metadata, not prompt bodies. Neutral output waits at most five seconds during a write, then rechecks opt-in/session without indefinite queues or retry. At that Phase 5 checkpoint feedback was default OFF per connection; Phase 6 supersedes that preference lifetime with persisted opt-in, independently of AUTO. See [RGB result](../../docs/studio2-phase5/rgb-result.md).

Phase 5 static Display acceptance is restricted to the privately attested AD1E USB firmware 1.0 identity, profile 2 / Default / existing slot 9. The approved CODEX / TEST frame was written once in 436 native HID reports (excluding the ordinary handshake); the user separately confirmed the image displayed correctly. The Phase 5 normal UI permitted a single static image only in this scope, with live mode 2, explicit destructive confirmation of exact ranges and frame hash, and a fresh 83 preflight rejecting any binding other than 9/1/100/292. Old pixels were not backed up, no pixel rollback exists, other states may alias slot 9, and 04 saves the entire firmware configuration. Each later upload reports transaction acceptance and metadata verification and asks the user to inspect the actual image; it cannot infer visual success. Cancellation sends nothing; failure triggers no automatic retry or alternate address. GIF, all other physical Display targets, brightness setters, firmware operations and full configuration readback remain unavailable. The generic Java allocation is never a physical allocator. The private acceptance file is outside Git and the distributed build; another machine/device gets no automatic grant. See [Display result](../../docs/studio2-phase5/display-result.md) and [handoff](../../docs/studio2-phase5/handoff.md).

The [Phase 5 finish review](../../docs/studio2-phase5/ui-review.md) returned SHIP after the bounded English-text correction. The final [36-capture matrix](../../docs/studio2-phase5/evidence/ui) covers six states in EN/RU/ZH × Light/Dark at the reviewed 1024-DIP layout, with the overwrite dialog separate. These are offline UI replays, not physical proof or accessibility/DPI certification. The established blue/Segoe UI WPF world and supplied image assets are unchanged; no HTML/CSS detector ran.


Phase 6.1 normal flow retains Profiles → Keymap Apply → optional focused Test key → Display import/Fit/Crop/upload → Lighting preference → Codex Ready/service startup. Developer mode and Diagnostics are unnecessary. Editing and active keyboard profiles stay distinct. Valid K2–K4 edits keep Apply actionable in normal BLE use while no key write or USB operation is busy. Missing, ambiguous or unaccepted USB gives recovery guidance, sends no write and preserves BLE/draft. With one exact private accepted USB candidate, Apply acquires exclusive USB ownership, validates live identity/firmware and key acceptance, then confirms selected device/profile/key, shortcut/label and no-backup/global-save consequences. Only the minimal selected-key shortcut/label/global-save plan executes. USB is released and prior BLE restored once when the operation still owns the session/selection and no explicit disconnect intervened; there are no write retries. K1 stays locked.

Local receipts match transport/device-path hash, firmware, target and exact value. A matching accepted USB receipt now remains explicitly “Written to AhaKey (USB)” after BLE restoration; it is historical accepted-write evidence, never BLE readback or Synced. This supersedes Phase 6's blanket unavailable label after returning to BLE. A matching local baseline without an exact USB/current-connection receipt still reports write status unavailable. Failed-or-uncertain writes retain the draft and show localized recovery immediately below the inspector title through the existing polite live region. `KeymapViewModel.Feedback` derives failure from exact-current-key/value `PhysicalState`, avoiding stale failure text on a new edit. Optional Test key requires matching receipt and active physical profile; only matching focused input with the same live session/receipt marks behavior verified. The [bounded finish review](../../docs/studio2-phase61/ui-review.md) ships this resolved near-title finding only, not a renewed whole-surface audit or complete Phase 6.1 acceptance.

Phase 6 persists cached Display imports, original names, target and Fit/Crop choice. Physical Upload remains one non-GIF frame for Codex / Default / existing slot 9, privately attested AD1E USB firmware 1.0, active mode 2. Unsupported content/targets remain inert with an explanation. A supported prepared image without eligible connection/mode sends nothing and opens Device with guidance. The concise Cancel / Replace image dialog exposes replacement/no-restore consequences; raw ranges, hash and commands remain in collapsed Advanced. Fresh 83 preflight, global-save consequences and separate image inspection remain unchanged.

Lighting now leads with a saved per-profile/integration preference; the nine-event custom matrix, numeric Preview, brightness and counters move to collapsed Advanced. Default Codex mapping sends 01 for UserPromptSubmit/PreToolUse and 00 for Stop/TaskCompleted/SessionEnd/PermissionRequest; unmapped events send nothing. Preference persistence never broadens accepted physical routes. Service autostart is a separate saved opt-in. Ready requires installed/configured/enabled/compatible evidence and trusted or not-required trust, while service running and saved lighting preference remain separately visible.

After the explicitly approved AD1E USB firmware 1.0 runtime 92 trial observed 2 → 1 → 2, an initially-off persisted profile-activation option allows explicit selection/Activate now to send 92 and verify 00. It does not save configuration, rewrite keys or prove power-cycle persistence. Activation errors remain visible on every page with editing/physical profile context and guidance to retry or disable activation in Profiles. See [profile-switch result](../../docs/studio2-phase6/profile-switch-result.md).

Historical Phase 6 record, superseded as a product-completion claim: the [Phase 6 physical acceptance](../../docs/studio2-phase6/physical-acceptance.md) recorded physical K2 focused observation, separate user confirmation of K2–K4, the CODEX / TEST image and real Codex RGB feedback. The user left lighting feedback and integration-service autostart on. Its restart retained both options, reconnected BLE and showed Codex Ready/service running; that restart did not prove BLE RGB capability or resolve the subsequently reported normal-use failures. The user's Phase 6 product rejection is authoritative; these older observations do not complete Phase 6.1 acceptance. Bounded desktop-installed Codex CLI detection remains available when Explorer PATH omits it, without changing hooks or native trust.

Phase 6.1 separately accepted BLE runtime RGB after one explicitly approved 01 → eight seconds → 00 trial on the exact private AD1E BLE identity and firmware 1.0. Both commands succeeded and the user separately observed lighting on then off. Only 00/01 are promoted; USB key/Display capability, other effects and AE1E do not inherit acceptance. Off does not restore an unknown prior animation. The trial is complete and must not be repeated for normal-product acceptance. See [BLE result](../../docs/studio2-phase61/ble-rgb-result.md). Lighting now shows the accepted live route or a direct Connect USB action, while keeping saved feedback and service-autostart preferences intact. Preference alone is not route evidence; output still requires explicit profile/integration opt-in, exact live identity/firmware, matching physical profile and an available operation gate. Hardware AUTO stays independent and initially off; trust is unchanged.

The Phase 6.1 normal published-app run recorded K2 label/shortcut/save ACKs through the exclusive USB handoff and subsequent BLE restoration. The missing-cable and failed-write recovery cases are isolated replay evidence, not physical cable removal or failure observations. A real Codex turn then produced accepted BLE 01/00 traffic; concurrent parent Codex activity is retained in the evidence. Protocol traffic is separate from visual confirmation: final K2 physical confirmation and observation of this normal Codex cycle remain pending, and the isolated BLE trial does not substitute for them. Published About, footer and Diagnostics were visually compared: all showed 2.0.0-alpha.6.1 and the same separately labeled modified build. Lighting was switched Off then On over accepted BLE, restoring saved On, and the process closed cleanly. Final package verification remains separate. EN/RU/ZH × Light/Dark offline captures and the review's scoped ship verdict do not certify hardware, screen readers, monitor DPI or Windows high contrast. No new physical work was performed for this documentation refresh.

Phase 9 bounded UI extension (2026-09-21): Settings adds Windows startup into the tray and unexpected-disconnect notifications. Closing hides the shell after the first-close explanation; native tray Open restores it and Exit is explicit. Device & Firmware adds package/readiness, component setup, operation and recovery states with advanced inspection/reinstall actions. These separately gated interfaces supersede historical no-firmware-UI statements only within Phase 9; they do not retroactively alter earlier acceptance. Package integrity, live device eligibility, operation completion and observed physical results remain distinct. The finish reviewer closed 5/5 findings with SHIP at finding scope. Real firmware flashing, clean-VM installation and whole-product acceptance remain pending. See [bounded UI documentation](../../docs/studio2-phase9/ui-documentation.md).
