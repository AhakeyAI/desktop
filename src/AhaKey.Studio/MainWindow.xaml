<Window x:Class="AhaKey.Studio.MainWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:views="clr-namespace:AhaKey.Studio.Views" xmlns:controls="clr-namespace:AhaKey.Studio.Controls" Title="{Binding L[AppTitle]}" Icon="Assets/AhaKeyStudio.ico" Width="1360" Height="920" MinWidth="1024" MinHeight="720" WindowStartupLocation="CenterScreen" Background="{DynamicResource AppBackground}" Foreground="{DynamicResource TextPrimary}" FontFamily="Segoe UI" FontSize="{StaticResource Type.Body.Size}" UseLayoutRounding="True" SnapsToDevicePixels="True">
 <Grid Background="{DynamicResource AppBackground}">
  <Grid.ColumnDefinitions><ColumnDefinition Width="212"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
  <Border Background="{DynamicResource Surface}" BorderBrush="{DynamicResource Border}" BorderThickness="0,0,1,0">
   <Grid Margin="12,20,12,16">
    <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
    <StackPanel Orientation="Horizontal" Margin="8,0,0,24"><Image Source="Assets/AhaKeyStudio.png" Width="28" Height="28" Margin="0,0,8,0" RenderOptions.BitmapScalingMode="HighQuality"/><TextBlock Text="{Binding L[AppTitle]}" Style="{StaticResource BrandTitle}" VerticalAlignment="Center"/></StackPanel>
    <ListBox Grid.Row="1" ItemsSource="{Binding Navigation}" SelectedItem="{Binding NavigationSelection}" AutomationProperties.Name="{Binding L[Home]}">
     <ListBox.ItemContainerStyle><Style TargetType="ListBoxItem" BasedOn="{StaticResource {x:Type ListBoxItem}}"><Style.Triggers><DataTrigger Binding="{Binding Key}" Value="Integrations"><Setter Property="Margin" Value="0,20,0,2"/></DataTrigger><DataTrigger Binding="{Binding Key}" Value="Device"><Setter Property="Margin" Value="0,20,0,2"/></DataTrigger></Style.Triggers></Style></ListBox.ItemContainerStyle>
     <ListBox.ItemTemplate><DataTemplate><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="20"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><controls:NavigationIcon Kind="{Binding Key}" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=ListBoxItem}}" Width="18" Height="18"/><TextBlock Grid.Column="1" Text="{Binding Label}" Margin="10,0,0,0" Style="{StaticResource Body}"/></Grid></DataTemplate></ListBox.ItemTemplate>
    </ListBox>
    <Button Grid.Row="2" Command="{Binding OpenSettingsCommand}" HorizontalContentAlignment="Stretch" Margin="0,12,0,16" Padding="10,8" AutomationProperties.Name="{Binding L[Settings]}"><Button.Style><Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}"><Setter Property="BorderThickness" Value="0"/><Style.Triggers><DataTrigger Binding="{Binding IsSettings}" Value="True"><Setter Property="Background" Value="{DynamicResource Selection}"/><Setter Property="Foreground" Value="{DynamicResource Focus}"/></DataTrigger></Style.Triggers></Style></Button.Style><StackPanel Orientation="Horizontal"><controls:NavigationIcon Kind="Settings" Width="18" Height="18" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" Margin="0,0,12,0"/><TextBlock Text="{Binding L[Settings]}" Style="{StaticResource Body}"/></StackPanel></Button>
    <TextBlock Grid.Row="3" Text="{Binding Version.Version}" Style="{StaticResource Caption}" Margin="8,8,0,0"/>
   </Grid>
  </Border>
  <Grid Grid.Column="1">
   <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
   <Border Background="{DynamicResource Surface}" BorderBrush="{DynamicResource Border}" BorderThickness="0,0,0,1" Padding="24,12">
    <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
     <ComboBox Width="216" HorizontalAlignment="Left" VerticalAlignment="Center" ItemsSource="{Binding Profiles.Cards}" SelectedItem="{Binding Profiles.Selected}" DisplayMemberPath="SelectorName" AutomationProperties.Name="{Binding L[CurrentProfile]}"/>
     <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center"><controls:DeviceStatus Width="180" Margin="16,0,16,0"/><Button Visibility="{Binding ShowWrite,Converter={StaticResource BoolVisibility}}" Style="{StaticResource PrimaryButton}" Command="{Binding WriteCommand}" IsEnabled="{Binding CanWrite}" ToolTip="{Binding WriteHint}" ToolTipService.ShowOnDisabled="True"><TextBlock Text="{Binding WriteLabel}" MaxWidth="176" TextWrapping="Wrap" TextAlignment="Center"/></Button></StackPanel>
    </Grid>
   </Border>
   <Border Grid.Row="1" Margin="24,16,24,0" Padding="12" CornerRadius="8" Background="{DynamicResource SurfaceRaised}" Visibility="{Binding HasError, Converter={StaticResource BoolVisibility}}"><TextBlock Text="{Binding ErrorText}" Foreground="{DynamicResource Danger}" Style="{StaticResource Body}" AutomationProperties.LiveSetting="Polite"/></Border>
   <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled"><Grid Margin="24,24,24,24" MaxWidth="1120" HorizontalAlignment="Stretch"><views:HomePage Visibility="{Binding IsHome, Converter={StaticResource BoolVisibility}}"/><views:SettingsPage Visibility="{Binding IsSettings, Converter={StaticResource BoolVisibility}}"/><views:KeymapPage DataContext="{Binding Keymap}" Visibility="{Binding DataContext.IsKeymap, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource BoolVisibility}}"/><views:BleDevicePage DataContext="{Binding Ble}" Visibility="{Binding DataContext.IsRealDevicePage, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource BoolVisibility}}"/><views:BleDiagnosticsPage DataContext="{Binding Ble}" Visibility="{Binding DataContext.IsRealDiagnosticsPage, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource BoolVisibility}}"/><views:IntegrationsPage DataContext="{Binding Integrations}" Visibility="{Binding DataContext.IsIntegrations, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource BoolVisibility}}"/><views:LightingPage DataContext="{Binding Controls}" Visibility="{Binding DataContext.IsLighting, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource BoolVisibility}}"/><views:DisplayPlannerPage DataContext="{Binding DisplayPlanner}" Visibility="{Binding DataContext.IsDisplay, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource BoolVisibility}}"/><views:FoundationPage Visibility="{Binding IsFoundation, Converter={StaticResource BoolVisibility}}"/></Grid></ScrollViewer>
  </Grid>
 </Grid>
</Window>
