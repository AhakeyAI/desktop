# 用例 C1（阻塞 — Studio Runtime 离线）

- 开始/结束时间：2026-08-27 21:51 替换 Agent；21:51:23 协商 current；21:52–22:14 尝试 5.7 Studio apply；未下发配置包
- 操作时点与动作：
  1. 用 `3b08d82` Release agent 替换 `/tmp/ahakey-hil-bin`；旧 PID 10092 退出；新 PID 76134
  2. 0x99 重新协商为 protocol v3 / current，primary `0..<276`，reclaim `276..<284`
  3. 启动 `/tmp/ahakey-hil-studio/AhaKey Studio.app`（不覆盖 `/Applications`）
- Studio 返回：主界面「Runtime 离线」。未执行「写入键盘」。
- Agent 日志段：
  - 协商：`raw/caps14-renegotiate.txt`、`raw/agent-hil.log` 13:51:19Z–
  - 替换：`raw/agent-swap-caps14.txt`
- 对照：`raw/c1-xpc-transport-probe.txt` — libxpc smoke handshake+snapshot exit 0
- 设备现象：键盘仍由 HIL Agent 持有（status 轮询、LED 0x90）；屏幕配置未改
- 截图：`raw/c1-studio-before.png`（引导/Agent 提示）、`raw/c1-studio-runtime-offline.png`
- 结论（阻塞）：0x99/current 已闭合。C1 的 5.7 apply 被 Studio `NSXPCConnection` 客户端与 Agent `libxpc` server 传输不匹配挡住。不在 HIL 卡内改业务代码。见草稿 `HIL-CONFIG-STUDIO-XPC-CLIENT`。
