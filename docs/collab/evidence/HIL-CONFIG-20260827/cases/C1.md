# 用例 C1（未通过 — apply 部分提交）

## 历史阻塞（2026-08-27，已由 15B 解除传输层）

- Studio 曾「Runtime 离线」；libxpc smoke 成功。见 `01-agent-swap-and-c1.md`、15B accepted @ `2403978`。

## 本轮（2026-08-28 12:18 CST / 04:18Z）

- 开始/结束：用户在临时 Studio 清掉 Claude/Codex/Kimi 图后，将 Cursor 四态+默认设为仓库 `cursor.gif`（4402B PNG 冒充 gif）并点「写入键盘」。
- 环境：HIL Agent PID **76134**（`3b08d82` Release，sha `392d5e06…`）；临时 Studio PID **28233** `/tmp/ahakey-hil-studio`；`/Applications` 未覆盖。04:11Z 有一次 BLE 重连并重新 0x99（v3/current，primary `0..<276`，reclaim `276..<284`）。
- Studio 返回：`部分完成：Runtime 报告部分步骤未写入（—）。可再次点击写入重试。`
  （`resumablePartial`/`failedWithPartialCommit` 分支；`messageCode` 为空故显示「—」。WAL 实为 **failedWithPartialCommit**。）
- Runtime WAL：
  - operation `49A143EC-6931-475B-AC36-FD26E8830412`
  - `completed_steps=3` / `total_steps=7`
  - 已确认：`resource:mode1-default`、`resource:mode1-set0-working`、`resource:mode1-set0-waiting`（同一 digest `1ac411d1…`，4402B，mediaType gif）
  - `runtime_sync_baselines` 空
- Agent 日志：`raw/c1-apply-partial-20260828.txt`（`配置事务 49A143EC… 受理/执行结果：failedWithPartialCommit`）。期间无独立 0x80/0x82 文本行（被 LED 轮询淹没）。
- 设备现象：基础配置未落 baseline；Cursor 三槽资源步已确认，屏幕是否已换成 cursor 图需用户目视（本记录不伪称上屏成功）。
- 结论：**C1 不通过**。图片资源事务有部分确认，整包未完成、无 revision/baseline。C2–C6 暂停。产品缺口另卡 `STUDIO-OLED-ENCODE-AND-PARTIAL-APPLY`。未在 HIL 卡改业务代码。不断电、未关蓝牙。

## 本轮（2026-08-28 13:11 CST / 05:11Z）— 用户目视 Logo+灯效，WAL 仍部分失败

- 用户授权在 HIL 环境替换 Agent（不再保留 PID 76134），修 bind 后 base 步失败。
- 重试写入：资源步可完成；`base:mode:1` **deviceRejected**（绑图/灯效已上屏，WAL 整步仍失败）。Studio 仍「部分完成」。用户目视：**Cursor Logo + 灯效已上设备**。
- 根因：`baseConfigurationProgram` 在 current 路径绑图后仍发 **0x98**（`PICTURE_WRITE_END`）。0x80 会话已用 0x9B/0x81 收尾，固件拒绝 0x98 → 步失败。另：空 OLED 模式若排在有图模式前，空 `0x97` 会挡掉 Cursor base。

## 本轮（2026-08-28 13:33 CST / 05:33Z）— 已部署去掉 0x98 的 HIL Agent，C1 待用户再点写入

- 代码：`AhaKeyDeviceProgramSteps` 有绑图时只发 0x97 + `saveConfig`，**不再发 0x98**；planner 有图模式的 base 先于空 OLED 模式。
- HIL Agent PID **14735**，sha256 `800adda002e83bec315b418ee02cde5fae8a5fd267e4b6f0f0fb74c4e5ce588b`，Developer ID `P2VFVRZK7P`。正式 `lab.jawa.ahakeyconfig.agent` 仍 **disabled**。临时 Studio `/tmp/ahakey-hil-studio` PID **38296**。
- 05:33:23Z 0x99：protocol v3 / current；日志 `primary 0..<276`，`factory reserved 276`，**reclaim `284..<292`**（同日更早 HIL 为 `276..<284`；原始应答含 `14 01 1C 01 24 01`）。
- 结论：C1 **仍未绿**（尚未用新 Agent 跑通 `completed`）。C2 起等本次写入成功后再开。C4/C5 未提示。

## 本轮（2026-08-28 13:39 CST / 05:39Z）— 去掉 0x98 后改卡在 0x97；用户续报 UX

- 新 Agent PID 14735 两次写入：`E3669637`（05:35:26Z）、`FC7DE8E7`（05:36:15Z）。均为 **failedWithPartialCommit 3/7**，已确认仍是 Cursor 三资源；`message_code` 空 → Studio「部分完成（—）」。`base:mode:1` **deviceRejected**，日志 **`配置命令 0x97 被设备拒绝 status=3`**。`0x04 save` 未执行。`runtime_sync_baselines` 仍 0。
- 前一次 `88B28D2E` 用户取消：`failedWithoutWrites` 0/7（体感慢）。
- 用户目视/使用：
  1. 关机再开机，上次写入的图消失，必须重写（与 save 未跑、无 baseline 一致；**不是**已执行的 HIL C4 用例）。
  2. 写入过程中任务状态变化切图时，屏幕**不出现** `uploading pic`。
  3. 出现过 `uploading pic` 时字节数一直是 **0,0**，不随进度变，最后一次性刷掉。
- Agent 在 apply 期间仍发 **0x90**（例：05:35:09 LED 2、05:35:21 LED 5），会抢 OLED 显示。
- 摘录：`raw/c1-0x97-reject-20260828.txt`。C1 仍不通过。C2–C6 不执行。

## 本轮（2026-08-28 13:52 CST / 05:52Z）— 根因定位 + 客户端两处修复已部署

对照固件源 `ahakeyconfig-latest-task-gif/CH582m_vibe_coding_BLE_keyboard-master`：

- **0x97 status=3 = 固件 EEPROM journal 环 fail-closed**。唯一出口是 `save_active_sets_now() != 0` → `eeprom_write_data()` 在「下一槽已占用且 `latest_record_offset` 在同半区」时 `return 1`；失败不推进 `addr_offset`，环写满一轮后**永久卡死**。属 WBS-1.4R5 范围，不刷机无法消除。
- **关机丢图** = `0x95` 的 `save_key_bound_data()` 早于固件置 `ai_oled_set_magic`；开机 `sanitize_key_bund_data()` 见 magic 不符即 `memset(ai_pic_set)`。只有显式 `0x04` 才会落盘带 magic 的 `key_bund`，而 0x04 原先排在 0x97 之后，永远跑不到。
- **`uploading pic` 恒 `0,0` 且只刷一次** = `show_picture_upload_screen()` 打印 `sector/7, sector`（sector 序号，非字节），且只在 `first_chunk` 绘制。
- **切图时不显示 uploading** = 固件 `0x90` 会 `update_claude_oled()` 全屏重绘覆盖上传屏，而 Agent 在 apply 全程仍发 0x90。

客户端修复（已部署）：mapper 把 `saveConfig` 移到 `0x97` 之前（绑定仍早于 save）；Agent 事务窗口内暂缓 0x90、窗口结束补发。`swift test` 499 通过。HIL Agent PID **41299**，sha256 `6764470839362795cb99c513eaf6cc3eee9f95b4f6b76820b7bc1dc9821cbdda`，正式 label 仍 disabled。

预期：图与绑定在 0x04 落盘，**关机不再丢图**；上传屏不再被 0x90 覆盖。**0x97 仍将 status=3**，WAL 仍 `failedWithPartialCommit` → C1 按现判据仍不通过。遗留事项立卡 `DEVICE-PERSIST-AND-UPLOAD-UX`。

## 本轮（2026-08-28 14:06 CST / 06:06Z）— 用户复验：0x90 抑制生效，其余三项未解决

operation `4B91457B`，**failedWithPartialCommit 3/7**，同样 `配置命令 0x97 被设备拒绝 status=3` → `base:mode:1 deviceRejected`。baseline 仍 0。

- **已生效**：日志出现 `LED 状态 5/3/2: 配置事务进行中，暂缓下发`（06:05:47、06:06:00 ×2），apply 期间不再下发 0x90。
- **未解决（用户复验）**：① 关机仍丢图；② 写入中不显示进度；③ 键盘屏幕仍 `0,0`；④ Studio 界面长时间显示 `0/7`。

> **口径更正**：本次 operation `4B91457B` 的**真实 WAL 是 `3/7`**（`runtime_confirmed_steps` 确有 `resource:mode1-default` / `set0-working` / `set0-waiting` 三条）。`0/7` 只是大资源步期间的**界面观感**（进度按步发布、步内无粒度），或上一条已终结 operation 的残留显示。**不得记作 WAL 0/7。**

### 纠正上一条的预期判断（R2 结论不完整）

`0x04` 本次**执行成功**（无 0x04 拒绝日志，0x97 排在其后且已抵达），带 magic 的 `key_bund` 已落盘，`sanitize_key_bund_data()` 不会再清空绑定。但关机仍丢图，因为开机顺序是：

1. `load_key_bound_data()` 恢复 `key_bund`（含 `active_ai_pic_set`）；
2. **随后** `if (data_in_fram._reserved[0] == ACTIVE_SET_JOURNAL_MAGIC)` 用 journal 里的 mask **覆盖** `active_ai_pic_set`。

journal 环已卡死、`0x97` 永远写不进去，所以第 2 步用的是**陈旧 mask**。若陈旧 mask 中 mode1 的位为 1，开机就会激活空的 set B → 屏幕无图。即：**关机丢图与 0x97 status=3 是同一个 journal 缺陷的两个表现，0x04 兜不住**。

- Studio 界面长时间显示 `0/7` 是**客户端投影粒度**缺口，不是 WAL 值：整包 7 个 WAL 步，前 3 步是大体积资源上传，进度只按步发布、步内无粒度 → 观感「停在 0/7 后跳变」。真实 WAL 为 3/7。C-1 先用已用时文案止血，真实字节进度归 C-2。
- 键盘 `0,0` 与不刷新仍是固件 `show_picture_upload_screen()`（打印 sector 序号、只在首块绘制）。
