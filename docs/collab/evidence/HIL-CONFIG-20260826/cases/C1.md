# 用例 C1（部分通过 — 旧路径）

- 开始/结束时间：2026-08-26 ~20:10–20:14
- 操作：用户在新版 Studio（热替换至 /Applications 的 b7798ba 构建）下发配置（含图片），
  新版 agent 经正式 label + MachServices 登记运行。
- Studio 返回：「配置已成功写入键盘」；键盘图片写入成功（用户确认真机现象）。
- Agent 侧核对：runtime-store（runtime.sqlite3 / resources/）在 apply 时段无任何写入——
  Studio 当前走的是旧直接 BLE 写路径，未经 WBS-5.6 的 XPC apply + 事务引擎。
- 结论：旧路径真机回归通过（新客户端+新 agent 无回归）；但 C1 对新事务引擎的覆盖
  不成立，需 XPC 驱动或 WBS-5.7 落地后重跑 C1。
